import 'package:flutter/material.dart';

class PalmResultPage extends StatelessWidget {
  const PalmResultPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('손금 분석 결과')),
      body: const Center(child: Text('손금 결과 화면 — v1.1 예정')),
    );
  }
}
